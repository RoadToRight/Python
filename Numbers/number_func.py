import math
round(2.4) # works like nomal roundoff
print(round(2.4)) # always upper roundoff If the decimal part is exactly .5, it rounds to the nearest even number (not always rounding up or down).

